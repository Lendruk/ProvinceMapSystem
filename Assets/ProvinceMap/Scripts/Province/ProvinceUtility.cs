using System.Collections;
using System.Collections.Generic;
using UnityEngine;



public static class ProvinceUtility
{
    /*
    public static float CalculateProvinceHeight(this PolydrawObject province)
    {
        if (!province.isValid) return -1f;
        MeshRenderer meshRenderer = province.gameObject.GetComponent<MeshRenderer>();
        return meshRenderer.bounds.size.y;
    }
    public static float CalculateProvinceWidth(this PolydrawObject province)
    {
        if (!province.isValid) return -1f;
        MeshRenderer meshRenderer = province.gameObject.GetComponent<MeshRenderer>();
        return meshRenderer.bounds.size.x;
    }
    public static float CalculateProvinceSize(this PolydrawObject province)
    {
        if (!province.isValid) return -1f;
        MeshRenderer meshRenderer = province.gameObject.GetComponent<MeshRenderer>();
        return -1;
    }
    */
}

